<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EE.Hillewaere.Views.StocklistView">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="lvCategoriesLists" ItemTapped="lvCategoriesLists_ItemTapped" ItemsSource="{Binding Categories}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.ContextActions>
                                <MenuItem Clicked="mnuCategoryEdit_Clicked" CommandParameter="{Binding .}" Text="Edit" />
                                <MenuItem Clicked="mnuCategoryDelete_Clicked" CommandParameter="{Binding .}" Text="Delete"
                                          IsDestructive="True"/>
                            </ViewCell.ContextActions>
                            <StackLayout>
                                <Label Text="{Binding Name}"
                                       VerticalOptions="CenterAndExpand"
                                       HorizontalOptions="StartAndExpand" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>